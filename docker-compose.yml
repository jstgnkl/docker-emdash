name: emdash

services:
  emdash:
    image: jstgnkl/emdash:latest
    container_name: emdash
    ports:
      - "4321:4321"
    volumes:
      # Persist SQLite database and uploaded media across restarts
      - emdash-data:/app/templates/blog/data
      - emdash-uploads:/app/templates/blog/uploads
    restart: unless-stopped

volumes:
  emdash-data:
  emdash-uploads:
